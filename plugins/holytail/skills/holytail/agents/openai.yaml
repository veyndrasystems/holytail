interface:
  display_name: "Holytail"
  short_description: "Check behavior around an active minimizer"
  default_prompt: "Use $holytail:holytail to check an active minimizer against accepted behavior."

policy:
  allow_implicit_invocation: true
